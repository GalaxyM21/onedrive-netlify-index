[build.environment]
  NPM_FLAGS = "--version"
  NPM_CONFIG_LEGACY_PEER_DEPS = "true"
[build]
#    publish = ".dist"
    command = "npm i --package-lock-only && npm fund && npm audit fix --force && npm install pnpm && pnpm run build"
